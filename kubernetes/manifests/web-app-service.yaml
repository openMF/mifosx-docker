apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: /home/azureuser/kompose --file docker-compose.yml convert
    kompose.version: 1.34.0 (cbf2835db)
  labels:
    io.kompose.service: web-app
  name: web-app
spec:
  ports:
    - name: "80"
      port: 80
      targetPort: 80
  selector:
    io.kompose.service: web-app

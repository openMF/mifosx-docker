apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: fineract-server-claim0
  name: fineract-server-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
